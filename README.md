# Slog & Open Tel Sample Go Application

Quick demo on setting up Open Telemetry in a go with HTTP client and server services.

To run this demo type `just` with [just](https://github.com/casey/just) installed.

Then make a request using `just request`.

Finally oppen the Jaeger UI with `just jaeger`.
