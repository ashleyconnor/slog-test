version: "3.9"

services:
  service-a:
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        SERVICE: service-a
    ports:
      - "8080:8080"
    environment:
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
      OTEL_SERVICE_NAME: service-a
    depends_on:
      - service-b
      - otel-collector

  service-b:
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        SERVICE: service-b
    ports:
      - "8081:8080"
    environment:
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
      OTEL_SERVICE_NAME: service-b
    depends_on:
      - otel-collector

  otel-collector:
    attach: false
    image: otel/opentelemetry-collector-contrib:0.112.0
    command: ["--config=/etc/otelcol/config.yml"]
    volumes:
      - ./collector.yml:/etc/otelcol/config.yml
    ports:
      - "4318:4318" # OTLP/HTTP
      - "4317:4317" # OTLP/gRPC
      - "8889:8889" # Prometheus metrics

  jaeger:
    attach: false
    image: jaegertracing/all-in-one:1.41
    environment:
      COLLECTOR_OTLP_ENABLED: true
    ports:
      - "16686:16686" # UI
    depends_on:
      - otel-collector
